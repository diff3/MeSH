-- Creates character uniq save file

if not MeSH then
  MeSH = {}
end

if not MeSH['frameScale'] then
  MeSH['frameScale'] = 1;
end

if not MeSH['lockButton'] then
  MeSH['lockButton'] = true;
end

if not MeSH['lockFrame'] then
  MeSH['lockFrame'] = true;
end

if not MeSH['showBindingText'] then
  MeSH['showBindingText'] = true;
end
